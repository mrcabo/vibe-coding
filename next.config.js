/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    // Add any additional configurations for your app here
  }
  
  module.exports = nextConfig